export default function PlanPanel({state}:{state:any}){const p=state.plan;if(!p)return <div className="empty">No response plan yet. Trigger the flood simulation to activate the agent network.</div>;return <><div className="plan-head"><div><span className="eyebrow">RECOMMENDED RESPONSE</span><h2>Response Plan <em>v{p.version}</em></h2></div><span className={`badge ${p.status==='APPROVED'?'green':'orange'}`}>{p.status}</span></div>{p.changes?.length>0&&<div className="change-box"><b>⚠ SITUATION CHANGED</b><span>Road A-B is now BLOCKED. Previous plan is no longer optimal.</span>{p.changes.map((c:string)=><small key={c}>↳ {c}</small>)}</div>}<div className="recommendations">{p.recommendations.map((r:any,i:number)=><div className="recommendation" key={r.action}><div className="priority">{r.priority}</div><div><strong>{i+1}. {r.action}</strong><p>{r.reason}</p><small>{r.affected_area} {r.assigned_resource&&`· ${r.assigned_resource}`}</small></div><span className="confidence">{r.confidence}%</span></div>)}</div><div className="why"><span className="eyebrow">EXPLAINABILITY</span><h3>Why this plan?</h3>{p.explanation.map((x:string)=><p key={x}>✓ {x}</p>)}</div></>}
